version: '3'
services:
  cars_api:
    build: .
    ports:
    - "8000:8000"
    command: python -m uvicorn cars_api.main:app --reload --host 0.0.0.0 --port 8000
    environment:
      - SQLALCHEMY_DATABASE_URL
